alias vi='nvim'
alias vim='nvim'

alias ls='ls -G'
alias -- -='cd -'        # Go back

eval "$(hub alias -s)"

alias node='node --use_strict'

if [ -f ~/.dotfiles.local/aliases ]
then
  source ~/.dotfiles.local/aliases
fi
